# Night Bloom poster task

Create an original `poster.svg` and `design-notes.md` from `brief.md`.

The poster must be 1200 by 1600, accessible SVG, contain every fixed event fact, use only inline vector artwork, and preserve the supplied palette. The notes must record the chosen direction, exact palette, and asset provenance.
