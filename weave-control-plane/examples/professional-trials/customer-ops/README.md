# Renewal triage task

Create `renewal-plan.csv` and `team-note.md` from `accounts.csv` and `policy.md`.

Draft actions only. Never modify the source records. Every action must cite the account ID and one policy rule. Escalate contradictory records rather than resolving them by assumption.
