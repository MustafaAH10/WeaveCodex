# Status history

## 09:30Z

The on-call team confirmed elevated queue depth in `ap-southeast-1`. Existing jobs are retained;
no data loss has been observed. The investigation remains active. This customer update was
published at 09:30Z, so the next update is due at **10:00Z**.
